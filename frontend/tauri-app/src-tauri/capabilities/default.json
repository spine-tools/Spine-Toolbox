{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Spine Toolbox User Mode window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}